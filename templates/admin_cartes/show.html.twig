{% extends 'base-backend.html.twig' %}

{% block title %}Cartes{% endblock %}

{% block body %}
<a href="{{ path('app_admin_cartes_index') }}" class="btn btn-primary btn-sm mt-3">
Retour a la Liste
    
</a>
    <h1>Cartes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carte.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ carte.titre }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ carte.texte }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ carte.imageName }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content-between align-center">
    <a href="{{ path('app_admin_cartes_edit', {'id': carte.id}) }}" class="btn btn-success">
        <i class="align-middle" data-feather="edit-2"></i>
    </a>
    
    {{ include('admin_cartes/_delete_form.html.twig') }}
    </div>

    

    
{% endblock %}