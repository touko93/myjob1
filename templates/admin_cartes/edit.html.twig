{% extends 'base-backend.html.twig' %}

{% block title %}Edit Cartes{% endblock %}

{% block body %}
<a href="{{ path('app_admin_cartes_index') }}" class="btn btn-primary btn-sm">Retour a la liste</a>
<div class="d-flex justify-content-between align-items-center">
<h1 class="mt-3">Edition Cartes</h1>

{{ include('admin_cartes/_delete_form.html.twig') }}

</div>


{{ include('admin_cartes/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}

