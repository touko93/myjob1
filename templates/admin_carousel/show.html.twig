{% extends 'base-backend.html.twig' %}

{% block title %}Carousel{% endblock %}

{% block body %}
<a href="{{ path('app_admin_carousel_index') }}" class="btn btn-primary btn-sm mt-3">
Retour a la Liste
    
</a>
    <h1>Carousel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carousel.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ carousel.imageName }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ carousel.text }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ carousel.title }}</td>
            </tr>
            <tr>
                <th>Tag</th>
                <td>{{ carousel.tag }}</td>
            </tr>
            <tr>
                <th>ImageName1</th>
                <td>{{ carousel.imageName1 }}</td>
            </tr>
            <tr>
                <th>ImageName2</th>
                <td>{{ carousel.imageName2 }}</td>
            </tr>
            <tr>
                <th>ImageName3</th>
                <td>{{ carousel.imageName3 }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex justify-content-between align-center">
    <a href="{{ path('app_admin_carousel_edit', {'id': carousel.id}) }}" class="btn btn-success">
        <i class="align-middle" data-feather="edit-2"></i>
    </a>
    
    {{ include('admin_carousel/_delete_form.html.twig') }}
    </div>


   

   
{% endblock %}