<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenu!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        <!-- CSS only -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/styles.min.css') }}">
        <link rel="stylesheet" href="{{ asset('css/lightbox.min.css') }}">
        {% block stylesheets %}
        
        {% endblock %}

    
    </head>
    <body>
        <nav class="navbar navbar-expand-lg bg-dark navbar-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#" title="Acceuil"> 
</a><img src="{{asset("images/logo_Myjob.svg")}}" alt="logo Myjob" width="60" height="60" class="d-inline-block align-text-top">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarNav">
      <ul class="navbar-nav ms-auto ">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_front')}}">Acceuil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link " href="#">Candidat</a>
        </li>
        <li class="nav-item">
          <a class="nav-link " href="#">Entreprise</a>
        </li>
        <li class="nav-item">
          <a class="nav-link " href="{{ path('app_front_formation')}}">Formation</a>
        </li>
        <li class="nav-item">
          <a class="nav-link " href="{{ path('app_front_annonce')}}">Annonce</a>
        </li>
      </ul>
      {# BIENVENUE - ADMIN - DÉCONNEXION - CONNEXION #}
      <li class="nav-item ms-auto">
      {% if is_granted("ROLE_USER") %}
      <div class="d-flex align-items-center">
          <div class="text-white">Bienvenue {{ app.user.email }}</div>
          {% if is_granted("ROLE_ADMIN") %}
          <a class="nav-link me-1" href="#" target="_blank">Admin</a>
          {% endif %}
      <a class="nav-link" href="#">Mon Compte</a>
          <a class="btn btn-outline-success" href="#">Déconnexion</a>
      </div>
      {% else %}
      <div class="d-flex align-items-center" >
      <a class="btn btn-info me-1" href="#">Inscription</a>
      <a class="btn btn-outline-info" href="#">Connexion</a>
      {% endif %}
      </li> 
      </div>

    </div>
  </div>
</nav>
      <main class="container d-flex justify-content-center py-5">
      <div class="example-wrapper">
    <form class="d-flex align-items-center"  role="search">
        <input class="form-control me-2" type="search" placeholder="Recherche" aria-label="Search">
        <button class="btn btn-outline-dark" type="submit">Recherche</button>
        </form>
</div>
      </main>
        {% block body %}
        {% endblock %} 
      
      
      

        
        <!-- JavaScript Bundle with Popper -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
      <script src=" {{ asset('js/lightbox.min.js') }}" ></script>
        {% block javascripts %}
            
        {% endblock %}
    </body>
</html>
